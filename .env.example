# Firebase web config lives in src/lib/firebase.ts (not secret — safe to
# commit, same as any Firebase client config). No env vars needed for auth.

# Backend API URL used by the dev-server proxy for /api requests
VITE_API_URL=http://localhost:8080

# Set to "false" to test the real auth/paywall flow in dev
VITE_PREVIEW_MODE=
